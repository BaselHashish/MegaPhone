<% if object && object.errors.size > 0 %>
  <div id="errorExplanation" class="text-danger text-center">
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>